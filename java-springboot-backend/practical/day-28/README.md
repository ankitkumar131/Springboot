# Practical — Day 28 (answer key)

Type these files yourself under `~/springboot-practice/day-28` while following
[the lesson](../../day-28-production-apis/README.md).

```bash
mvn spring-boot:run
curl http://localhost:8080/api/hello
curl http://localhost:8080/actuator/health
```
