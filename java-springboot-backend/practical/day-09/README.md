# Practical — Day 09 (answer key)

Type these files yourself under `~/springboot-practice/day-09` while following
[the lesson](../../day-09-first-rest-api/README.md).

```bash
mvn spring-boot:run
# then the curl commands in the lesson
```
