# Practical — Day 26 (answer key)

Type these files yourself under `~/springboot-practice/day-26` while following
[the lesson](../../day-26-authorization-security/README.md).

```bash
mvn spring-boot:run
curl -i -u user:password http://localhost:8080/api/admin/stats
curl -i -u admin:password http://localhost:8080/api/admin/stats
```
