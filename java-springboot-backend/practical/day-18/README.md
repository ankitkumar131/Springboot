# Practical — Day 18 (answer key)

Type these files yourself under `~/springboot-practice/day-18` while following
[the lesson](../../day-18-postgresql-fundamentals/README.md).

```bash
psql -h localhost -U app -d app_db -f schema.sql
```
