# Practical — Day 19 (answer key)

Type these files yourself under `~/springboot-practice/day-19` while following
[the lesson](../../day-19-springboot-postgresql/README.md).

```bash
mvn spring-boot:run
curl -s -X POST http://localhost:8080/api/employees -H 'Content-Type: application/json' -d '{"name":"Ada","email":"ada@example.com"}'
```
