# Practical — Day 14 (answer key)

Type these files yourself under `~/springboot-practice/day-14` while following
[the lesson](../../day-14-spring-data-jpa/README.md).

```bash
mvn spring-boot:run
# watch SQL in the console, then:
mysql -u root -psecret -e 'SELECT * FROM app_db.users'
```
