# Practical — Day 17 (answer key)

Type these files yourself under `~/springboot-practice/day-17` while following
[the lesson](../../day-17-advanced-jpa/README.md).

```bash
mvn spring-boot:run
curl 'http://localhost:8080/api/products?page=0&size=5&sort=name,asc'
```
