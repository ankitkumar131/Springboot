# Practical — Day 05 (answer key)

Type every file under `~/springboot-practice/day-05` following
[the lesson](../../day-05-spring-boot-fundamentals/README.md).

```bash
mvn spring-boot:run
curl http://localhost:8080/api/hello
```
