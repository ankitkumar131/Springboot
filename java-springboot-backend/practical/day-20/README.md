# Practical — Day 20 (answer key)

Type these files yourself under `~/springboot-practice/day-20` while following
[the lesson](../../day-20-advanced-postgresql/README.md).

```bash
mvn spring-boot:run
# Flyway creates flyway_schema_history
```
