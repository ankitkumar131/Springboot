# Practical — Day 15 (answer key)

Type these files yourself under `~/springboot-practice/day-15` while following
[the lesson](../../day-15-mysql-crud-jpa/README.md).

```bash
mvn spring-boot:run
curl -s -X POST http://localhost:8080/api/users -H 'Content-Type: application/json' -d '{"name":"Ada","email":"ada@example.com"}'
```
