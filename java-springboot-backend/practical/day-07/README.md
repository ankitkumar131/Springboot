# Practical — Day 07 (answer key)

Type these files yourself under `~/springboot-practice/day-07` while following
[the lesson](../../day-07-spring-mvc/README.md).

```bash
mvn spring-boot:run
curl http://localhost:8080/api/demo/ping
curl http://localhost:8080/api/demo/users/5?view=full
curl -H 'X-Request-Id: abc' http://localhost:8080/api/demo/who
```
