# Spring Security / JWT interview questions

AuthN vs AuthZ, SecurityFilterChain, BCrypt, CSRF vs stateless, CORS preflight, JWT structure, exp, Bearer filter, 401 vs 403, hasRole prefix, logout with JWT.

Days 24–26.
